```
定义Raft节点的基本数据结构（状态、持久化状态、日志条目等）。

实现Raft节点的初始化。

实现Raft节点的选举逻辑（RequestVote RPC和处理程序）。

实现Raft节点的日志复制（AppendEntries RPC和处理程序）。

实现Raft节点的状态持久化。

实现Raft节点的日志压缩（快照）功能（可选，但重要）。

实现Raft节点之间的通信（网络层）。

```


# 阶段1：基础数据结构和状态定义
## 步骤1.1：定义基本数据结构
## 步骤1.2：定义RaftNode类的基本状态
# 步骤2.1：实现RequestVote RPC处理